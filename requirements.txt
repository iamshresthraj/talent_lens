--extra-index-url https://download.pytorch.org/whl/cpu

# Core ranking pipeline and Gradio sandbox dependencies
numpy>=2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
PyYAML>=6.0.0
sentence-transformers>=2.2.0
transformers>=4.30.0
torch

# Sandbox demo app only (sandbox/app.py)
gradio>=4.36.0
pypdf
# llama-cpp-python is commented out to prevent compilation/OOM issues on HF. 
# It falls back to template mode automatically in reasoning.py.

# Test suite only
pytest>=8.0.0

